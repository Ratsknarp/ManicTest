
name: Build Unsigned IPA

on:
  push:
    branches: [ "debug" ]
  pull_request:
    branches: [ "debug" ]
  workflow_dispatch:

jobs:
  build:
    name: Build Unsigned IPA
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 'latest'

      - name: Fix Project Format
        run: |
          sed -i '' 's/objectVersion = 77/objectVersion = 56/' ManicEmu.xcodeproj/project.pbxproj
          sed -i '' 's/preferredProjectObjectVersion = 77/preferredProjectObjectVersion = 56/' ManicEmu.xcodeproj/project.pbxproj

      - name: Archive Build (Unsigned)
        run: |
          xcodebuild clean archive \
            -project ManicEmu.xcodeproj \
            -scheme "ManicEmu" \
            -configuration Release \
            -archivePath build/ManicEmu.xcarchive \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            SWIFT_OPTIMIZATION_LEVEL="-Onone" \
            IPHONEOS_DEPLOYMENT_TARGET=17.4

      - name: Create IPA from Archive
        run: |
          # Copy the .app from the archive
          cp -R build/ManicEmu.xcarchive/Products/Applications/ManicEmu.app .

          # Package the app into an IPA
          mkdir -p Payload
          cp -R ManicEmu.app Payload/
          zip -r ManicEmu.ipa Payload
          rm -rf Payload ManicEmu.app

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ManicEmu.ipa
          path: ManicEmu.ipa
          retention-days: 90
